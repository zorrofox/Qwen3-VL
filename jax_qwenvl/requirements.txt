# Qwen3-VL MaxText SFT 依赖
# 训练框架通过 MaxText 提供，本目录只需数据管道依赖

# MaxText（含 JAX、Flax、Orbax、Pallas 等）
# 安装：pip install -e /path/to/maxtext-qwen3vl

# 数据管道（llava_dataset.py）
datasets>=2.18.0
pillow>=10.0.0
google-cloud-storage>=2.19.0

# 权重转换（convert_qwen3vl.py）
safetensors>=0.4.0
torch>=2.0.0       # CPU only，仅用于加载 safetensors
tqdm>=4.66.0
